import Component from '@ember/component';
import { get, set } from "@ember/object";
import { typeOf } from '@ember/utils';

//import { assect } from "@ember/debug";
//import $ from 'jquery';

export default Component.extend({
  didInsertElement() {
    this._super(...arguments);

  }
});
